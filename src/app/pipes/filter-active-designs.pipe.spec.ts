import { FilterActiveDesignsPipe } from './filter-active-designs.pipe';

describe('FilterActiveDesignsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActiveDesignsPipe();
    expect(pipe).toBeTruthy();
  });
});
